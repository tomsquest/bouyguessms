# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib
/bouyguessms
dist/

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Private files (with password)
Notes.md
e2e_test.go
